{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; [ -n \"$f\" ] && [ -f \"$f\" ] && npx --no-install biome check --write --files-ignore-unknown=true \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
